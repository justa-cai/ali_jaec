# Training and evaluation are pure PyTorch; onnx/onnxruntime are only needed
# for export_onnx.py, infer.py and the c/test_equiv.py acceptance test.
torch>=2.4
numpy>=1.24
soundfile>=0.12
onnx>=1.16
onnxscript>=0.2        # required by torch.onnx dynamo export
onnxruntime>=1.17      # ONNX DFT (used for the FFTs) needs >= 1.17
